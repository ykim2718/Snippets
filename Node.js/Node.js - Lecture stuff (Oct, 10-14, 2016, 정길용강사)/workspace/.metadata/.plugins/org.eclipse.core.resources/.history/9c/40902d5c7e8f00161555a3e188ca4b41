var EventEmitter = require('events');
var event = new EventEmitter();

// Add event methods
event.on('timed', function(data){
	console.log('on timed event raised.', data);
});
event.once('timed', function(data){
	console.log('on timed event raised.', data);
});
event.addListner('timed', function(data){
	console.log('on timed event raised.', data);
});