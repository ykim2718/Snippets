var childProcess = require('child_process');

var child, file;

// node hotdeploy.js net/tcpserver.js
function runChild(){
	file = process.argv[2];
	if (file) {
		child = childProcess.spawn('node', [file]);
	} else {
		throw new Error('실행할 파일을 지정해주세요. node hotdploy 파일명')
	}
}

